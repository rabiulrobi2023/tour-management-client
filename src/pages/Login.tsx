import { LoginForm } from "@/modules/LoginForm";

const Login = () => {
  return (
    <div className="flex mx-auto p-0 min-h-screen  justify-center bg-gray-800">
      <LoginForm></LoginForm>
    </div>
  );
};

export default Login;
